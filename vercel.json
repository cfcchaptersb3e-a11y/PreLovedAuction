{
  "buildCommand": "prisma generate && prisma db push --skip-generate && npm run db:indexes && next build",
  "crons": [
    {
      "path": "/api/cron/close",
      "schedule": "0 3 * * *"
    }
  ]
}
